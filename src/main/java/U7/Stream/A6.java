package U7.Stream;

public class A6 {
  public static void main(String[] args) {
    // Implementa la clase Socio con los atributos dni, nombre, fechaNacimiento, fechaAlta (ambos de
    // tipo LocalDate), cuota y numFamiliares (número de familiares del socio). Además de un
    // constructor, implementa los métodos equals(), compareTo (basados en el dni) y toString().
    // Crea una tabla con 5 socios y, a partir de ella, un Stream con los socios:
    // Ordenados por DNI.
    // Con una cuota mayor que 100 €.
    // Cuyo nombre empieza por 'A'.
    // En todos los casos, muestra por pantalla los elementos del Stream.

  }
}
